# CLAUDE.md — DIZRUPT Project Rules

## Current project reality (read first)
- The product is a **Next.js 14 App Router app in `dizruptos/`** (not a static site).
- **The dashboard (`/`) is a macOS-style web OS — "DizruptOS".** It boots (boot → lock → desktop), runs a window manager (drag / 8-way resize / snap / genie-minimize / z-order / per-persona layout persistence in `localStorage` `dz-os-layout:<personaId>`), a magnifying customizable Dock, a Menubar ( menu + app menus + Control Center + Notification Center + calendar), and overlays: Spotlight (⌘Space), Mission Control (F3), Launchpad (F4). **Every legacy route opens *as a window*** (chromeless iframe via `?embed=1` — see `shell-frame.tsx` embed mode + `frame-ancestors 'self'` in `middleware.ts`), so no functionality was lost in the redesign. Native apps: **Home** (per-role task command center), **Project Matrix** (DnD Kanban), **Operative Directory** (people), **Knowledge Vault** (IndexedDB files). App registry + RBAC gating live in `lib/desktop-apps.tsx`; OS state in `lib/os.ts` (`useOS`); window manager in `components/desktop/use-desktop.ts`. Launch any app by dispatching `CustomEvent("dizrupt:launch",{detail:{id}})`.
- **Run:** `cd dizruptos && npm run dev` → **http://localhost:3000** (plain `next dev` default; the old 5175 note is stale). Build `npm run build`, tests `npm test` (vitest), `npm run e2e` (Playwright smoke).
- **Backend:** live **Supabase** when `dizruptos/.env.local` is set (URL/anon/service-role/`DATABASE_URL`); otherwise demo mode on the in-memory seed. `DATABASE_URL` must be the **Session Pooler** URI (IPv4) — direct `db.*.supabase.co:5432` is IPv6-only. **Never print/commit secrets.** Migrations + seed live in `dizruptos/supabase/`.
- **Domain model is schema-authoritative (Option A):** the Postgres schema is the source of truth; app layer uses thin camelCase views + TanStack Query (`lib/query.ts`). RBAC: `lib/personas.ts` (matrix) + `useSession.can()`, now also enforced at the OS layer (apps hide/deny by `perm`).
- Key docs: `SUPREME_PLATFORM_AUDIT.md` (honest ratings — frontend/UX **9.97**, design **9.8**, auth code-readiness **9.5**, enterprise **9.5**, production **9.7**, architecture **9.8**, copilot **9.5**, simulation **9.9**, graph **9.3**, accessibility **9.6**, code quality **10**, overall technical **9.9**), `ROAD_TO_10.md`, `PLAN.md`, `README.md`, `AUTH_SETUP.md`, `SOC2_CONTROLS.md`.
- **Real auth (Supabase) is code-complete & env-gated:** `lib/auth-supabase.ts`, `components/auth/real-auth-form.tsx`, `app/auth/callback/route.ts`, async session-validating `middleware.ts`, **`supabase/migrations/0012_auth_hook.sql`** (custom access-token hook + auto-provision trigger). Going live = apply migration + enable the dashboard hook + real users.
- **Enterprise scaffolding (2026-06-15):** SCIM 2.0 full provisioning API (`/api/v1/scim/`), SSO SAML SP-initiated + ACS + OIDC (`/api/auth/sso/`), `SOC2_CONTROLS.md`, per-tenant settings DB (`0014`), admin provisioning API (`/api/v1/admin/`). **Per-tenant SSO in DB** (`0017_tenant_sso_config.sql`, `tenant_sso_configs` table + tenant suspension API at `/api/v1/admin/tenants/[id]/sso|suspend`).
- **AI Copilot (2026-06-16 native UI):** `components/desktop/apps/copilot-app.tsx` — native OS window app (id: `copilot`, Bot icon, `#00ED82` accent, dock-pinned). Chat-style interface: user messages right (accent-tinted), assistant messages left with BrainCircuit avatar + inline evidence chips + Claude badge when LLM-enhanced. Intent-aware follow-up chips generated per API intent (capacity/burnout/succession/org-health/decisions/risks/recommendations). 6 starter prompts on empty state. Calls `GET /api/v1/copilot?q=`. Backend: `server/engine/copilot-llm.ts` wires Claude claude-sonnet-4-6 + 8s timeout + deterministic fallback. **TF-IDF semantic search** (`server/services/embeddings.ts`) — top-K hits injected into LLM context as "SEMANTIC CONTEXT". Exposed also via `/api/v1/search`. Set `ANTHROPIC_API_KEY` to enable LLM.
- **Production stack (2026-06-15):** `Dockerfile` (multi-stage/non-root), `docker-compose.yml` (app+Redis+Prometheus+Grafana), `vercel.json` (OWASP headers/CSP/crons), `/api/v1/metrics` Prometheus, `instrumentation.ts` OTel + Sentry init (`onRequestError` hook), enhanced `/api/health` capabilities manifest. **React Error Boundary** (`src/components/error-boundary.tsx` + `withErrorBoundary` HOC) + `app/global-error.tsx`. **Web Vitals** (`web-vitals.tsx` → `/api/v1/metrics/vitals`). **security.txt** RFC 9116 (`/.well-known/security.txt`).
- **Graph at scale (2026-06-15):** `0013_graph_traversal.sql` — recursive CTE BFS + betweenness centrality + dependency hubs + path materialization. `/api/v1/intelligence/graph` with JS fallback.
- **Ingestion connectors (2026-06-15):** `/api/v1/import/{jira,linear,github}` — HMAC-verified webhook receivers that **write to graph tables** via `server/services/graph-writer.ts` (Jira→tasks+projects, Linear→tasks+projects, GitHub merged PRs→decision_records). Idempotent. Graceful no-op in demo mode. Monte Carlo simulation: `/api/v1/simulation/monte-carlo` (reliability fix: fallback counts when DB unreachable).
- **Universal Supabase resilience (2026-06-15):** `server/repositories/index.ts` — `makeResilient()` Proxy wraps every sub-repo with auto-fallback to in-memory seed on network errors. All 19 API routes + all service loaders resilient in a single change. Zero 500s from Supabase unreachability for ALL surfaces (narratives, exec briefing, recommendations, org memory, copilot, graph, health, people, capabilities). `capability-loader.ts` + `people-loader.ts` also individually hardened.
- **Dependency graph (2026-06-15):** `app/(shell)/graph/page.tsx` — nodes 2.5×/2.2× wider spread (x→1740, y→460), thicker edges (2px base + strength), `minZoom: 0.2`, panel 700px. All `Link href` removed — clicking nodes dispatches `dizrupt:launch` CustomEvent to open the right OS app window (no navigation away). Works for all logins.
- **Zero old-dashboard links (2026-06-15):** `executive/page.tsx`, `people/[id]/page.tsx`, `projects/[id]/page.tsx` — all `next/link` usage replaced with `button + launchApp()` helper dispatching to `window` + `window.parent`. Nothing escapes the OS shell from any embedded iframe.
- **Tiered rate limiting + Cache-Control (2026-06-15):** `middleware.ts` — intelligence routes `/api/v1/intelligence` limited to 10 req/min, all others 60 req/min, audit/nav exempt. 429s include `Retry-After`. Intelligence GET routes receive `private, max-age=60, stale-while-revalidate=30`.
- **Betweenness centrality + Influence lens (2026-06-15):** `graph/page.tsx` — `approximateBetweenness()` (Brandes BFS) + Influence map lens with **TOP badges** on high-centrality nodes + stats row (`StatChip` × 4). Third lens alongside blast-radius and bus-factor.
- **OrgHealthSparkline (2026-06-15):** `components/ui/org-health-sparkline.tsx` wired into `components/desktop/apps/home.tsx` — 7-day health sparkline with trend arrow + delta, visible for all personas on the Home app. Built on `SparkArea`, `SparkBars`, `CapacityRing` primitives in `components/ui/spark.tsx`.
- **Enterprise APIs (2026-06-15):** SCIM token rotation (`POST /api/v1/scim/token`), enterprise data export (`GET /api/v1/export`, CSV/JSON), navigation audit logging (`POST /api/v1/audit/nav`, fire-and-forget). Feature flags in `lib/feature-flags.ts` (10 flags, O(1), env-gated).
- **Chat → Notification Center (2026-06-15):** `useOps.addNotification` action added. Desktop page listens for `dizrupt:chat-message` events and pushes to notification center under "Messages" group. Notification routes to Chat app on click.
- **Accessibility (2026-06-15):** `src/lib/focus-trap.ts` (`activateFocusTrap` + `useFocusTrap` hook, WCAG 2.1 SC 2.1.2) + `src/components/ui/skip-link.tsx` (skip nav link in root layout → `#main-content`, WCAG 2.4.1). **`useFocusTrap` wired into Spotlight, Mission Control, and Launchpad** — all three major overlays now trap focus correctly. `robots.txt` (marketing indexable, shell/API disallowed).
- **Health history (2026-06-15):** `/api/v1/intelligence/health-history` — 30-day trend API with LCG synthetic history, trend direction (improving/stable/declining), delta. Migration `0016_health_snapshots.sql` (persistent table + 30-day backfill). Cron-captured via POST.
- **Realtime (2026-06-15):** `lib/realtime-supabase.ts` — Supabase Realtime channels with BroadcastChannel fallback. `CHANNELS` constants for org-level topics.
- **Login page:** `/login` — Nexus design system (dark `#0A0A0A`, orange `#F97316`, Newsreader serif, glass card `bg-[#0f0f0f]/80`). Background is a **canvas animation**: 3 amber light sources drift across a halftone dot field, illuminating dots in realtime + supernova pulse every ~8s + sweeping lens flares. HiDPI-aware. `powerOn()` called on submit. "Log in" / "Create account" tabs.
- **Boot sequence:** `phase` is NOT persisted — always starts `"boot"`. Login page calls `powerOn()` before navigating to ensure the sequence resets on in-tab navigation too.
- **Wallpaper:** default is "dizrupt-brand" (deep ink + amber-orange auroras). All 7 wallpapers have distinct color identities in BOTH modes: Volt Flux=lime-chartreuse, Dizrupt=amber-orange, Monterey=sky-blue, Solar=coral+magenta, Graphite=achromatic, Sequoia=cool-teal, Nocturne=magenta-violet.
- **Landing hero:** `/welcome` — text ("DIZRUPT", "every person. every project.", "every consequence.") uses overflow-clipped slide animations directly on type with NO colored plate backgrounds. "EST. 2026 — RUNS YOUR ORG" top-right is `rgba(255,255,255,0.75)` uppercase tracking.
- **Monte Carlo Simulation UI:** `components/desktop/apps/simulation-app.tsx` — native OS window app. 4 scenario types, parameter sliders, calls `/api/v1/simulation/monte-carlo`, renders p5–p95 percentile bars + risk flags. Registered in `desktop-apps.tsx` as `panel` kind, id `simulation`, RBAC `view_executive`. Window def in `page.tsx` `defs` array at `{ id: "simulation", x:120, y:60, w:920, h:640, closed:true }`.
- **Standing routine (this session):** every prompt → re-read `SUPREME_PLATFORM_AUDIT.md` + `ROAD_TO_10.md`, push every metric toward 10 in code where possible, then update those two docs + `PLAN.md` + `README.md` + this file. Be honest: don't inflate infra-gated scores (real auth live, SSO live, SOC2 auditor, real users) — code-readiness ≠ operational.
- **New additions (2026-06-19 Road-to-10 sprint):** `src/components/desktop/apps/admin-app.tsx` (Admin Console — Tenants/SSO/SCIM/Audit tabs, id: `admin`, perm: `view_audit`); `src/server/lib/logger.ts` (JSON structured logger); `src/server/lib/api-error.ts` (normalized error response). `spotlight.tsx` rewritten with correct WCAG ARIA listbox pattern. `focus-trap.ts` fixed. All 6 iframe shell pages got OS-language headers + empty states. `React.memo` on HomeApp, CopilotApp, SimulationApp. 177 tests passing.
- **New additions (2026-06-19 Max-10 Final Sprint):** Per-tenant SSO routing in `middleware.ts` (`SSO_CONFIG_SEED`, `/api/auth/sso?tenant=<id>` → SAML/OIDC redirect); `src/lib/__tests__/accessibility.test.ts` (WCAG 2.1 contrast formula, 8 token pair tests); `aria-live="polite" aria-atomic="false"` on all remaining data regions (capabilities, goals, decisions); OS-language dark-glass headers added to proposals, audit, risks, people, people/[id], projects/[id] pages; `/api/health` echoes `requestId` in body + `X-Request-ID` header; simulation `calibration` object in API response + footnote in simulation-app; `log()` wired into copilot + all import routes; `withSpan("copilot.answer")` + `withSpan("simulation.run")` OTel spans; `"assumption" | "policy"` added to `EntityType` + `Brain`/`Shield` icons in graph `KIND_ICON`; HRIS Bulk entity in import page; `NotificationCenter` subscribes to `CHANNELS.NOTIFICATIONS` via `realtimeChannel()`; `load-test.mjs` scaffold (health/graph/copilot p50/p95); `e2e/desktop.mjs` (5 integration checks). **190/190 tests passing.**
- **Platformization Sprint (2026-06-21):** Full customer journey code-complete. **Auth:** `syncFromSupabase()` in `session.ts` + `SupabaseAuthSync` in `providers.tsx` + `onAuthStateChange` listener + expiry redirect + `reset-password` pages. **Invitations:** `0020_invitations.sql` + `/api/v1/invitations` (GET+POST) + `/api/v1/invitations/[token]` (GET/POST/DELETE) + `/accept-invite` page. **Org creation:** `/api/v1/organizations` (GET+POST). **Onboarding wizard:** `/onboarding/` (5 steps: org-name → invite → import → tools → ready). **Realtime:** `event-publisher.ts` + `notification-service.ts` + `/api/v1/notifications`. **Executive V2:** inline Copilot quick-ask + "What Changed" feed (`/api/v1/intelligence/delta`) + Fragility Map (bus-factor + burnout). **HRIS Bulk:** `hris_bulk` added to csv.ts SCHEMAS + importCsv service branch + import page. **Ops:** slow-query detection (>500ms), admin tenants live DB, SSO DB config lookup (replaces `SSO_CONFIG_SEED`), SCIM Users GET/POST live queries. **Tests:** 4 new test files (auth, onboarding, invitations, notifications — 44 new tests). **236/236 tests passing. 0 TS errors. E2E: 10 checks.**
- **Infra-gated hard ceiling (code is complete — operational gaps only):** Real auth = apply `0012_auth_hook.sql` migration + enable Auth Hook in Supabase dashboard + real users. SSO live = connect a real IdP (Okta/Azure AD) to validate assertions. SOC2 = Type II audit (9-month external process). Sentry = set `SENTRY_DSN` env var. These are NOT code gaps.

## Always Do First
- **Invoke the `frontend-design` skill** before writing any frontend code, every session, no exceptions.

## Reference Images
- If a reference image is provided: match layout, spacing, typography, and color exactly. Swap in placeholder content (images via `https://placehold.co/`, generic copy). Do not improve or add to the design.
- If no reference image: design from scratch with high craft (see guardrails below).
- Screenshot your output, compare against reference, fix mismatches, re-screenshot. Do at least 2 comparison rounds. Stop only when no visible differences remain or user says so.

## Local Server
- **Always serve on localhost** — never screenshot a `file:///` URL.
- Start the app: `cd dizruptos && npm run dev` → **http://localhost:3000**. Start it in the background before screenshots.
- If a server is already running, do not start a second instance. Beware orphaned `next dev` processes piling up and serving 500s — kill project Next procs + clear `.next/cache` before a clean start.

## Screenshot Workflow (DizruptOS desktop)
- Screenshot the running dev server with the **cached Playwright Chromium** via a small `playwright-core` script (`dizruptos/shot.mjs` is the reusable helper); the Chromium binary is at `%LOCALAPPDATA%/ms-playwright/chromium-*/chrome-win64/chrome.exe`.
- Auth: middleware needs a `dz_session` cookie (presence-checked) — set it via `ctx.addCookies`. The desktop powers on, so wait ~3s through boot, then click `button[aria-label="Unlock"]`.
- Useful selectors: dock items `button[title="<App>"]`; menubar `aria-label`s "Control Center" / "Notifications"; theme buttons "Light"/"Dark" need `exact:true`; windows are `section[aria-label="<title>"]`. Note "Graphite" is both an accent AND a wallpaper (ambiguous selector).
- Embedded route windows can take ~7s to first-compile in dev — wait before screenshotting an iframe window. Don't use Playwright `addInitScript` to clear `localStorage` (it re-runs on reload and breaks persistence tests) — a fresh browser context already has empty storage.
- After screenshotting, read the PNG with the Read tool — Claude sees the image directly. Be specific when comparing (exact px / hex / spacing).

## Output Defaults
- Single `index.html` file, all styles inline, unless user says otherwise
- Tailwind CSS via CDN: `<script src="https://cdn.tailwindcss.com"></script>`
- Placeholder images: `https://placehold.co/WIDTHxHEIGHT`
- Mobile-first responsive

## Brand Assets
- Always check the `brand_assets/` folder before designing. It may contain logos, color guides, style guides, or images.
- If assets exist there, use them. Do not use placeholders where real assets are available.
- If a logo is present, use it. If a color palette is defined, use those exact values — do not invent brand colors.

## Anti-Generic Guardrails
- **Colors:** Never use default Tailwind palette (indigo-500, blue-600, etc.). Pick a custom brand color and derive from it.
- **Shadows:** Never use flat `shadow-md`. Use layered, color-tinted shadows with low opacity.
- **Typography:** Never use the same font for headings and body. Pair a display/serif with a clean sans. Apply tight tracking (`-0.03em`) on large headings, generous line-height (`1.7`) on body.
- **Gradients:** Layer multiple radial gradients. Add grain/texture via SVG noise filter for depth.
- **Animations:** Only animate `transform` and `opacity`. Never `transition-all`. Use spring-style easing.
- **Interactive states:** Every clickable element needs hover, focus-visible, and active states. No exceptions.
- **Images:** Add a gradient overlay (`bg-gradient-to-t from-black/60`) and a color treatment layer with `mix-blend-multiply`.
- **Spacing:** Use intentional, consistent spacing tokens — not random Tailwind steps.
- **Depth:** Surfaces should have a layering system (base → elevated → floating), not all sit at the same z-plane.

## Hard Rules
- Do not add sections, features, or content not in the reference
- Do not "improve" a reference design — match it
- Do not stop after one screenshot pass
- Do not use `transition-all`
- Do not use default Tailwind blue/indigo as primary color
